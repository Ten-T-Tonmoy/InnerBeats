import React, { useState } from "react";

const Assistant = () => {
  const [request, setRequest] = useState([""]);
  const [response, setResponse] = useState([""]);

  return <div className="bg-primary h-[100vh]">Assistant</div>;
};

export default Assistant;
